---
description: MAIN_RULES
alwaysApply: true
---

# MAIN_RULES

描述一些核心主规则，同时会引用一些子规则，是子规则的根节点。

## 一、日常规则
### 规则1：Always respond in Chinese-simplified
除非用户特别指定/说明，否则总是以中文回答。
### 规则2：不要修改scp-foundation的代码
除非用户特别指定/说明，否则不要修改scp-foundation的代码。

## 二、产品AI工作流

**产品工作方法**：  
当你在本仓库中处理与「产品需求、PRD、原型设计、测试用例、问题分析、设计审核」相关的任务时，请优先阅读 `.cursor/rules/产品AI工作流.mdc`。

该文档按以下结构组织，便于查阅与执行：

| 部分 | 内容 |
|------|------|
| 第一部分 | 核心原则（强约束）：主会话职责、禁止自创角色、用户硬确认、阶段签收提示 |
| 第二部分 | 角色与职责：4 个 subagent 定义、主会话职责、协作关系 |
| 第三部分 | 设计规则：目录与命名、原型图、问题分析报告、文档流转与对话输出 |
| 第四部分 | 工作流流程：4.1 正向需求（需求→PRD→原型→用例→审核→交付总结）、4.2 问题驱动（问题→分析→改进需求→正向流程） |

**何时自动启用产品 AI 工作流（触发规则）**：

- **路径触发（强制视为产品任务）**：  
  当用户指向的路径位于以下任意目录下的任意文件夹时，当前任务自动视为「产品任务」，主会话必须按 `产品AI工作流.mdc` 执行，不再额外询问；**但若用户在同一轮对话中以明确语句显式说明「本次不需要走产品AI工作流/不要按产品流程来/按普通对话处理即可」等同义表达时，主会话应尊重该选择，本轮任务不再套用产品 AI 工作流。**  
  - `产品设计/产品需求/`  
  - `产品设计/产品PRD/`  
  - `产品设计/产品原型图/`  
  - `产品设计/产品问题分析/`  
  - `产品设计/产品审核/`

- **语义触发（需要询问确认）**：  
  当用户使用的指令/描述中明显涉及「PRD、原型、用例、测试场景、问题分析、设计审核、问题、做一下这个需求、SOP 页面怎么设计」等关键词，但未显式指向上述目录时：  
  - 主会话应先用一句话向用户确认：  
    - 例如：「这个更像是‘产品任务’，是否希望按产品 AI 工作流来处理（需求→调研→PRD→原型→用例→审核）？」「这个更像是‘问题分析任务’，是否希望按产品 AI 工作流来处理（问题分析→确认为改进需求→调研→PRD→原型→用例→审核）？」  
  - 只有在用户明确回答「是/好/按产品工作流来」后，才进入产品 AI 工作流；否则按普通对话或其它处理方式处理。

- **目录创建与命名约定**：  
  - 产品相关 agent 可在 `产品设计/` 下**主动创建缺失的子目录**（如 `产品PRD/模块/模块_需求名/`、`产品原型图/模块/模块_需求名/` 等），须遵守 `产品AI工作流.mdc` 第三部分「设计规则」中的目录与命名规则。  
  - 用例文档默认与对应 PRD 存放在同一目录；版本迭代须使用时间戳子目录，不得通过修改「模块_需求名」目录名来区分版本。


## 三、网页信息获取
- **触发**：当用户要求从网页自动获取信息、抓取文档、爬取帮助页等时，主会话应阅读 `.cursor/rules/网页信息获取规则.mdc`。
- **调度**：使用 `mcp_task` 调用 `web-info-fetcher` subagent，prompt 中须包含 URL、任务描述、**结束条件**（用户未提供时先询问）。
- **存储**：抓取结果存入 `info/` 目录，结构见 `web-info-fetcher` skill 的 storage 规则。
