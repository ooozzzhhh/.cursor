---
description: 网页信息获取：主会话与 web-info-fetcher subagent 的协作规则
alwaysApply: false
---

# 网页信息获取规则

当用户需要从网页自动获取信息时，主会话调度 **web-info-fetcher** subagent，并遵守以下规则。

---

## 1. 主会话调用时须附带的信息

调用 `mcp_task` 且 `subagent_type: "web-info-fetcher"` 时，**prompt 中必须包含**：

| 必填项 | 说明 | 示例 |
|--------|------|------|
| **url** | 目标网页完整 URL | `https://help.sap.com/docs/...` |
| **任务描述** | 要获取什么、怎么操作 | 「抓取左侧目录中与 MRP 相关的 2 个章节」 |
| **结束条件** | 明确何时停止（见下） | 「抓取 2 个章节后停止」 |

| 可选项 | 说明 |
|--------|------|
| 来源_主题 | 用于 info 目录命名，未提供时由 subagent 从 URL/页面推断 |
| 存储偏好 | 是否只存摘要、是否存单章详情，默认两者都存 |

---

## 2. 结束条件（强制）

**主会话在构造 prompt 时，必须包含明确的结束条件**。若用户未提供，主会话应**先询问用户**，不得直接调度。

### 2.1 推荐结束条件类型

| 类型 | 示例 |
|------|------|
| 数量 | 「抓取 2 个章节」「最多 5 页」 |
| 内容 | 「抓取标题含『MRP』的章节」「直到遇到『附录』」 |
| 步数 | 「最多切换 10 次目录」 |
| 结构 | 「抓取当前页所有一级目录下的第一个子项」 |

### 2.2 用户未给结束条件时

1. 主会话**不调度** web-info-fetcher
2. 主会话回复用户：
   > 为避免无休止抓取，请先明确结束条件，例如：抓取 N 个章节、抓取到某关键词、或指定最大切换次数。
3. 待用户补充后，主会话再将结束条件写入 prompt 并调度

---

## 3. subagent 行为约束

- **无结束条件不执行**：若 prompt 中无结束条件，subagent 立即返回主会话，提示「请补充结束条件」
- **按 skill 执行**：遵循 `web-info-fetcher` skill 的工作流程与存储规则
- **结果写入 info/**：按 `info/来源_主题/yyyyMMdd/` 结构存储，并更新 `info/索引.md`

---

## 4. 存储与渐进式披露

- 存储规则见 `web-info-fetcher` skill 下的 [storage.md](../skills/web-info-fetcher/storage.md)
- 结构：`索引.md`（总览）→ `摘要.md`（本次概要）→ `章节_xxx.md`（详情）
- 主会话向用户转达时，可先给摘要路径，需要时再引导用户查看具体章节文件
