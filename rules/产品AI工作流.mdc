---
description: 产品AI工作流
alwaysApply: false
---
# 产品AI协同工作流与设计规则

本文件汇总本项目中所有产品相关 AI subagent 的整体协同工作流，以及与产品工作密切相关的设计规则。

---

## 第一部分：核心原则（强约束）

以下四条为全流程必须遵守的强约束，主会话与所有产品 subagent 均不得违反。

| 序号 | 原则 | 说明 |
|------|------|------|
| 1 | **主会话只负责分发，不参与执行** | 主会话仅负责理解需求、调用 subagent、传递上下文、转达摘要与待决策点；不得代替 subagent 撰写 PRD、编写调研、产出原型、编写用例、撰写审核报告或直接修改产品设计目录下的文档。所有可交付产出均由相应 subagent 完成。 |
| 2 | **严禁自创角色/任务** | 产品相关 AI 只允许以本文件约定的 4 个 subagent 身份工作，不得额外发明如「实现落地建议」「开发任务拆解」等新角色或新阶段。若确有需要，须在用户明确要求后，以「普通说明文字」形式输出。 |
| 3 | **阶段间必须有用户硬确认** | 从「PRD → 原型」「原型 → 用例」「用例 → 审核」的每一次跃迁，须先由主会话以明确语句确认，AI 才能自动执行下一步。仅说「继续」「往下走」不视为阶段签收。 |
| 4 | **阶段结束必须显式提醒签收** | 每个阶段结束时，AI 须用清晰话术提示用户「请先检查什么」「确认什么后才会进入下一阶段」，不得在用户尚未显式签收时自动推进。 |

---

## 第二部分：角色与职责

### 2.1 四个产品 subagent

| 角色 | subagent 标识 | 职责 |
|------|---------------|------|
| 产品经理 | product-manager | 查阅代码与数据库结构、沉淀产品知识库；需求分析与增量 PRD 产出，总体决策与取舍 |
| 产品前端设计师 | product-frontend-designer | 基于 PRD 生成 HTML 原型 + 设计说明 |
| 产品测试经理 | product-test-manager | 基于 PRD+原型生成用户用例/测试用例，以及输出问题分析报告 |
| 产品设计审核员 | product-reviewer | 对 PRD+原型做系统化设计审核和对标分析 |

### 2.2 主会话职责

- **负责**：理解用户需求 → 选择应调用的 subagent → 传入任务描述、时间戳、文档路径等上下文 → 向用户转达 subagent 摘要与待决策点 → 根据用户签收决定是否进入下一阶段。
- **不得**：代替任一 subagent 撰写或修改产出物，或对产品设计目录下的文档进行直接增删改。
- **例外**：在「4.1.6 主会话输出交付总结」步骤中，主会话可撰写并保存「交付总结_面向业务与领导」类文档；该文档为归纳与转述，不替代任何 subagent 产出的正式文档。此为**仅此一步**允许主会话直接写文档。

### 2.3 协作关系

- 调用 subagent 时，**始终由主会话负责调度与分发**。
- subagent 之间**不直接互相调用**，通过主会话的调用与用户的确认来串联。

---

## 第三部分：设计规则

### 3.1 目录与命名规则

#### 3.1.1 产品需求入口目录

- 路径：`产品设计/产品需求/模块/模块_需求名/`（如 `产品设计/产品需求/SOP/SOP_计划结果-计划指标前端重构/`）
- 用途：仅存放需求入口材料（`需求.txt`、业务方原始描述、截图、附件等）
- **禁止**：在该目录内存放正式 PRD、用例、审核报告等文档

#### 3.1.2 产品 PRD 目录

- 路径：`产品设计/产品PRD/模块/模块_需求名/`
- 用途：需求维度的总入口目录
- **禁止**：通过修改目录名（追加 `_v2`、日期后缀等）来区分多次迭代
- 版本管理：必须采用「时间戳子目录 + 功能历史」方式（见 3.1.6）

#### 3.1.3 产品用例目录

- 默认与 PRD 同目录：`产品设计/产品PRD/模块/模块_需求名/` 下，与 PRD 放在一起
- 文件命名建议：`模块_需求名_用例.md`
- **禁止**：将不同迭代的用例混写在同一文件中；每一轮迭代的用例须放在本轮的时间戳子目录中

#### 3.1.4 产品原型图目录

- 路径：`产品设计/产品原型图/模块/模块_需求名/`
- 要求：HTML 原型与 `设计说明.md` **必须按时间戳子目录分批次存放**，严禁在同一份原型文件上堆叠多轮大改

#### 3.1.5 产品问题分析目录

- 路径：`产品设计/产品问题分析/模块/模块_BUG_yy-mm-dd_xx/`（如 `SOP_BUG_2026-03-03_01`）
- 用途：存放问题分析报告及其附属材料

#### 3.1.6 时间戳子目录与功能历史（强制规范）

适用于：`产品需求`、`产品PRD`、`产品原型图`、`产品审核` 下的 `模块/模块_需求名/` 目录。

| 项目 | 规则 |
|------|------|
| 目录层级 | 第一层为「模块名」，第二层为「模块_需求名」或「模块_BUG_yy-mm-dd_xx」 |
| 版本管理 | 禁止直接堆叠多版本文件；须存在 `功能历史.md`，所有具体产物一律存放在以时间戳命名的二级子目录中 |
| 时间戳格式 | 14 位纯数字 `yyyyMMddHHmmss`（如 `20260303103045`） |
| 时间取值 | 本次需求任务在主会话中启动时的时间点；同一轮 PRD→原型→用例→审核 使用**完全相同的时间戳** |
| 功能历史 | 每次新增时间戳子目录时，必须同步更新 `功能历史.md`，写清时间戳、本轮目的、与上一轮差异 |
| 时间戳遗传 | 由主会话生成一次，显式传入所有 subagent；任何 subagent 严禁自行生成或更换时间戳 |

**严禁行为**：在同一时间戳目录中混写两轮不同方案；在旧文件尾部追加新一轮内容而不新建时间戳子目录；通过修改「模块_需求名」目录名规避规则。

**历史不规范目录**：一律视为「历史遗留，仅供只读参考」，不得在其中继续写入；新一轮迭代只能在符合规则的新时间戳子目录中写入。

#### 3.1.7 产品知识库目录（特殊规则）

- 路径：`产品设计/产品知识库/模块/类别（代码、数据库等）/...`
- **不适用**时间戳子目录规则；知识库文档以「始终保持最新、可被覆盖更新」为目标
- 更新时应在文档内注明「更新时间」与「本次调研范围」
- **严禁**在知识库目录下使用与 PRD 相同的时间戳子目录结构

#### 3.1.8 模块目录命名统一规则

| 模块 | 统一使用 | 禁止使用 |
|------|----------|----------|
| S&OP | SOP | S&OP |
| AMS | AMS | SCH |
| 其它 | MDS、SDS、IPS、MRP、MPS、DFP | 不得混用 |

若未来新增模块，须先在本规则中补充模块编码后，方可在 `产品设计/产品XXX/模块` 创建对应目录。

---

### 3.2 产品原型图设计规则

| 项目 | 规则 |
|------|------|
| 存放位置 | `产品设计/产品原型图/模块/模块_需求名/` |
| 文件格式 | 若用户无特别说明，仅生成自带样式可预览的 HTML 文件，含完整样式和脚本，可直接在浏览器打开 |
| 设计说明 | 须同步生成 `设计说明.md`，包含功能说明、UI 样式、UX 交互、实现注意事项；代码改动时须同步更新 |

---

### 3.3 产品问题分析报告规则

| 项目 | 规则 |
|------|------|
| 存放位置 | `产品设计/产品问题分析/模块/模块_BUG_yy-mm-dd_xx/` |
| 结构要求 | 至少包含三大章节：**现状**（现象与背景）、**问题**（根因分析）、**解决方案**（修复建议与验证思路） |

---

### 3.4 文档流转与对话输出规则

| 规则 | 说明 |
|------|------|
| 文档是唯一完整载体 | 所有详细内容须写入对应 Markdown 文档；对话回复只允许包含简明摘要（3～10 条）、关键文档路径、待决策点/下一步提示；严禁在对话中复制大段文档正文 |
| 各角色输出长度控制 | product-manager：现状/限制/风险摘要 + 知识库路径 + PRD 路径 + 关键点摘要 + 待决策清单；product-frontend-designer：原型路径 + 设计说明路径 + 设计要点；product-test-manager：用例路径 + 覆盖场景摘要 + 疑问点；product-reviewer：审核报告路径 + 审核结论摘要 + 下一步建议 |
| 禁止双维护 | 任一信息点只能在文档中完整维护一次；更新须先改 MD 文档，再在对话中给出新摘要与路径 |
| 引用文档方式 | 须给出相对路径；指向某轮迭代时须标明时间戳目录名 |

---

## 第四部分：工作流流程

### 4.1 正向需求流程（需求 → PRD → 原型 → 用例 → 审核）

#### 4.1.1 需求提出、现状调研与 PRD 撰写（product-manager 合并执行）

**用户输入**：自然语言描述业务需求，指定需求入口目录。

**主会话前置义务**：若需求入口目录已存在 `需求.txt` 等结构化说明，主会话须先自行阅读并理解，在调用 subagent 前显式传递：所属模块与功能范围、代码来源信息、已有设计稿/原型位置、是否需原型设计等。若需求文档指出代码不在当前本地仓库，主会话不得在后续提示中与之矛盾；若无法访问指定仓库/分支，须说明限制并要求 subagent 在调研结论中标注「仅基于当前可见代码/数据的初步结论」。

**流程**：主会话调用 product-manager。product-manager 必须按序完成：
1. **现状调研**：除非用户明确说明「本次不需要调研」或指定可复用知识库，先按 `code-implementation-research` skill 执行调研，查阅 `UHA/scp-foundation` 下对应模块代码、测试数据库（环境默认是 `scp-test`；具体库默认是`scp-XX`，例如`scp-sop`）表结构，将调研结果沉淀到 `产品设计/产品知识库/模块/...`，返回「现有能力、限制点、潜在改动影响」。
2. **PRD 撰写**：基于业务描述、调研结论（或指定知识库）、已有产品文档，在 `产品设计/产品PRD/模块/模块_需求名/` 下按 prd-writing 规范撰写增量 PRD。

**产品经理补充规则**：
- 知识库路径为优先阅读建议，不得假定某主题已有完备结论；若不存在对应文档，须说明「首次调研」并按规范新建。
- 须优先尊重主会话提供的代码来源信息；若无法访问指定仓库/分支，须显式声明并请求主会话完成分支切换或路径调整。

**输出要求**：PRD 摘要和完整文档路径；现状/限制/风险摘要及知识库路径；**关键待决策点列表**，显式请用户逐项给出决策或暂缓。

**用户确认**：用户须以明确同意语（如「同意按此 PRD 推进后续原型/用例」）确认 PRD 可作为原型/测试用例依据。

**流程守卫**：在未收到上述明确同意前，主会话**禁止**调用 product-frontend-designer、product-test-manager 或 product-reviewer。

> **本仓库特别提示**：`c:\Users\OZH\Desktop\UHA` 不是 Git 仓库；真实代码仓库位于 `c:\Users\OZH\Desktop\UHA\scp-foundation`。调用产品经理做代码调研时，如未特别说明，主会话应默认以该子目录为代码仓库根目录。

---

#### 4.1.3 原型设计（仅在 PRD 签收后执行）

**前置条件**：4.1.1 中用户已以明确语句确认「PRD 可以作为原型/测试用例依据」。

**流程**：product-frontend-designer 在 `产品设计/产品原型图/模块/模块_需求名/` 下生成 HTML 原型 + `设计说明.md`。产品经理对照 PRD 检查一致性。

**可跳过规则**：若需求为平台基础级/架构级/代码优化类（不新增前端页面、不涉及复杂交互、仅为轻量级文案/提示/行为优化或纯后端改动），可跳过原型设计，直接 4.1.1 → 4.1.4。须满足：
- a）产品经理向用户发起硬确认：「疑似不需要前端页面，是否跳过原型设计？」用户回答【是】或【跳过】；
- b）或需求文档中已明确「该需求不需要前端设计」「本次仅代码层优化，无需前端改动」。

**流程守卫**：若用户在原型阶段提出「需先调整 PRD」，须回到 4.1.1，由产品经理更新 PRD 并再次获得确认。

**阶段结束提示**：主会话须提示用户检查 PRD + 原型，并询问「是否同意按当前 PRD+原型进入用户用例/测试用例阶段」。仅当用户使用类似「同意按当前 PRD+原型进入用例/测试阶段」的语句时，才允许调用 product-test-manager。

---

#### 4.1.4 用户用例 / 测试用例（仅在 PRD+原型基本稳定后执行）

**前置条件**：4.1.1 和 4.1.3 已完成且用户确认，或 4.1.1 已完成且已按可跳过规则跳过原型设计。

**流程**：product-test-manager 基于 PRD（及如有则包括原型）生成用户用例/测试用例；在 PRD 的「用户用例」章节补齐，或新增 `模块_需求名_用例.md` 独立文档。产品经理审核用例覆盖度。

**阶段结束提示**：主会话须提示用户检查 PRD、原型、用例是否对齐，并询问「是否同意按当前 PRD+原型+用例进入设计审核阶段」。仅当用户使用类似「同意进入设计审核阶段」的语句时，才允许调用 product-reviewer。

---

#### 4.1.5 设计审核与对标（流程收尾与复盘）

**流程**：product-reviewer 在 `产品设计/产品PRD/`、`产品设计/产品原型图/` 中查找对应 PRD 与原型，在 `产品设计/产品审核/模块/模块_需求名/` 下输出审核报告（背景、目标、现状、问题、对标竞品、改进建议、「用户意见」、审核结论）。

**阶段结束提示**：主会话只能做两件事：1）提示用户阅读审核报告并在「用户意见」中给出反馈；2）等待用户表态「按当前方案进入开发」或「先按审核意见修订 PRD/原型/用例」。未收到明确结论前，AI 不得主动延伸「开发任务拆解」「实现方案细化」等额外任务。

---

#### 4.1.6 主会话输出交付总结（正向需求工作流最后一步）

**前置条件**：4.1.5 已完成，用户已阅读审核报告并给出「认可」或「需修订」表态。

**文档定位**：面向非技术人员（产品经理、业务领导），快速理解「会做哪些改造」及「期望效果」。

**内容要求**：分类分点说明改造内容（每类 1～2 个典型例子）；分类分点说明期望效果（每类 1～2 个典型例子）。以摘要、要点与例子为主。

**存放位置**：与 PRD 同时间戳目录，如 `产品设计/产品PRD/模块/模块_需求名/时间戳/交付总结_面向业务与领导.md`。

**执行主体**：主会话撰写并保存。此为**唯一**允许主会话直接写文档的步骤。

**流程收尾**：交付总结输出后，正向需求 4.1.1～4.1.6 闭环结束。

---

### 4.2 问题驱动流程（问题 → 分析 → 改进需求）

#### 4.2.1 问题收集与分析（问题入口强制规范）

**用户输入**：自然语言问题描述，或指向 `产品设计/产品问题分析/模块/模块_BUG_yy-mm-dd_xx/` 下已有目录的路径。

**建档规则**：若问题尚未建档，product-test-manager **不得自行创建目录或文件**，须先向用户询问（如「是否为本问题创建问题分析档案？」）。仅当用户使用含有「同意」「确认」「建档」「创建问题分析」等明确同意语句时，才能创建目录并写入问题分析报告；否则**只允许以对话形式给出结论**，严禁在文件系统中创建任何问题分析目录或文档。

**流程**：主会话先调用 product-manager 调研相关代码与数据库，在知识库中沉淀调研结论；再调用 product-test-manager 基于知识库与问题描述完成问题分析报告。

**流程守卫**：问题分析阶段**不得直接改 PRD 或调用 product-frontend-designer 出原型**。产品测试经理完成问题分析报告初稿后，主会话**只能**提示用户阅读报告并思考是否升级为改进需求；不得因用户说「继续」「往下走」就直接进入 PRD/原型/用例流程；须等到 4.2.2 中用户以明确语句确认后，才允许考虑发起增量需求。

---

#### 4.2.2 问题分析报告与用户确认（是否升级为需求的决策点）

**报告结构**：至少包含现状（现象与背景）、问题（根因分析）、解决方案（修复建议与验证思路）。

**流程**：测试经理提醒用户查阅；询问「该问题分析是否可以作为后续产品改进依据」。若用户认为不充分，可要求修订直至认可。

**用户确认**：仅当用户明确表示「同意按该问题分析作为改进依据」或类似语句时，才允许进入 4.2.3。单纯的「继续」「下一步」**不视为**签收。

**阶段结束提示**：主会话须提醒用户打开问题分析报告，确认现状/问题/解决方案是否准确，并明确询问「是否同意按该问题分析作为后续改进依据」。

---

#### 4.2.3 从问题到增量需求（问题闭环进入正向需求的唯一通道）

**流程**：用户认可问题分析后，可调用 product-manager「基于某问题分析报告发起改进需求」：
- 在 `产品设计/产品需求/模块/` 下创建 `模块_问题xxx改进` 入口目录；
- 在 `产品设计/产品PRD/模块/` 下按同名规则新建目录，撰写正式增量 PRD；
- 以问题分析报告为输入，在 PRD 中显式引用报告路径。
- 后续**完整执行** 4.1 正向需求流程（4.1.1～4.1.6）。

**流程守卫**：若用户撤回或修改对问题分析报告的认可，须先更新问题分析报告，再让用户确认，确认后产品经理才能基于最新版本发起或调整增量需求。

**与正向流程衔接**：问题改进需求**必须重新完整执行** 4.1 正向工作流，不得跳过任何用户确认节点或简化阶段。
